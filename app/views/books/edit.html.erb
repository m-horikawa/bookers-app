<body>
  <h1>Editing Book</h1>

<%= form_with model:@book, url: book_path, local:true do |f| %>

  <% if @book.errors.any? %>
  <%= @book.errors.count %> erros prohibited this book from being saved
    <ul>
      <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

  <div class="actions">
    <%= f.submit 'Update Book' %>
  </div>
<% end %>

  <div class="actions">
    <%= link_to "Show", book_path(@book.id) %>
    |
    <%= link_to "Back", books_path %>
  </div>
</body>
